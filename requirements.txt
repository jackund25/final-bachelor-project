# Dependensi untuk MENJALANKAN sistem.
#
# Berkas ini sengaja dijaga tetap kecil: seseorang yang hanya ingin menjalankan
# CDSS tidak perlu memasang TensorFlow atau RAGAS. Dependensi untuk mereproduksi
# angka laporan dan menjalankan pengujian ada di requirements-dev.txt, yang
# memuat berkas ini lewat `-r`.
#
#     pip install -r requirements.txt          # menjalankan sistem
#     pip install -r requirements-dev.txt      # + reproduksi angka + pengujian
#
# Versi numpy/pandas/scikit-learn WAJIB sama dengan backend/requirements.txt.
# scikit-learn 1.3.0 khususnya: bundle inferensi .pkl dibuat dengan versi itu dan
# tidak dapat dibuka oleh versi yang lebih baru.

# --- Bootstrap ---
pip>=24.2
setuptools>=70.0.0
wheel>=0.43.0

# --- Data & model prediksi ---
numpy==1.26.4
pandas==2.0.3
scikit-learn==1.3.0

# --- Konfigurasi ---
# src/config.py meng-import yaml dan berada pada jalur kritis seluruh sistem.
pyyaml==6.0.1
python-dotenv==1.0.0

# --- API ---
fastapi==0.115.9
uvicorn==0.49.0
pydantic==2.13.4

# --- Penelusuran (jalur produksi: rag.retrieval_mode = bm25) ---
# ChromaDB dipakai sebagai PENYIMPAN dokumen: _bangun_bm25 dan _hasil_dari_indeks
# hanya membaca documents/metadatas dari sqlite. Mesin vektornya tidak dijalankan
# pada mode leksikal.
chromadb==1.0.12
rank_bm25==0.2.2
#
# flashrank==0.2.10 DICABUT 25 Agustus 2026. rag.reranker.enabled = false, dan
# _muat_reranker mengembalikan nilai pada penjaga `if not self.reranker_enabled`
# SEBELUM `from flashrank import Ranker` — paket itu tidak pernah disentuh jalur
# mana pun. Untuk menyalakannya kembali, pasang ulang paketnya BERSAMA mengubah
# nilai config; salah satunya saja tidak cukup.

# --- Generasi ---
langchain-core==0.3.86
langchain-google-genai>=2.0.0

# --- Pengindeksan korpus + jalur pemulihan penelusuran ---
# TETAP DI SINI, dan sengaja TIDAK ada di backend/requirements.txt (dicabut dari
# sana 25 Agustus 2026). Root memikul dua tugas yang tidak dipikul lingkungan
# penyajian:
#
#   1. MEMBANGUN INDEKS. scripts/reingest_kb.py dan
#      MedicalKnowledgeBase.save_to_chroma() memanggil _build_embeddings dan
#      pemecah teks; tanpa keempatnya korpus 2.233 potongan tidak dapat dibangun
#      ulang, sehingga angka Bab VI berhenti dapat direproduksi.
#
#   2. MEREPRODUKSI LENGAN PEMBANDING. Jalur vektor dan hibrida tetap ada pada
#      src/rag/retriever.py sebagai kontrol eksperimen Tabel VI.3, yang mengklaim
#      ketiga cara diuji atas potongan yang sama. Klaim itu harus dapat diuji ulang.
#
# Pada mode bm25 keempatnya tidak pernah di-import saat melayani, sehingga di sini
# pun ia membebani disk saja, bukan memori runtime.
#
# torch dipatok eksplisit meski sentence-transformers sudah membawanya: di Windows
# ia harus di-import SEBELUM numpy/pyarrow untuk menghindari WinError 1114 pada
# c10.dll (lihat header skrip evaluasi).
langchain-chroma==0.2.4
langchain-community==0.3.24
langchain-text-splitters==0.3.8
sentence-transformers>=3.0.0
torch>=2.0

# ---------------------------------------------------------------------------
# DICABUT 23 Agustus 2026 — nol referensi di seluruh kode setelah UI Streamlit
# dihapus dan jalur penelusuran dibekukan ke BM25:
#
#   langchain==0.3.25      paket payung; hanya sub-paketnya yang pernah di-import
#   streamlit==1.25.0      UI Streamlit digantikan PWA Next.js di frontend/
#   plotly==5.15.0         pemakainya hanya UI Streamlit
#   seaborn==0.12.2        nol import
#   python-docx==0.8.11    nol import
#   tqdm==4.65.0           nol import
#
# Yang berpindah ke requirements-dev.txt: tensorflow, scipy, ragas,
# langchain-openai, nest-asyncio, ollama, langchain-ollama, matplotlib, PyPDF2,
# pytest, pytest-cov.
# ---------------------------------------------------------------------------
