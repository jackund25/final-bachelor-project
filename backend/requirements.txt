# Dependensi RUNTIME backend CDSS (FastAPI).
#
# CATATAN PENERAPAN. Berkas ini disusun untuk instans awan (Render) yang kemudian
# TIDAK dipakai; evaluasi dokter dijalankan dari laptop lewat ngrok, dengan
# lingkungan conda proyek — lihat docs/DEPLOY_LOKAL_NGROK.md. Daftar di bawah
# karena itu memerikan lingkungan penyajian MINIMAL yang cukup, bukan lingkungan
# yang benar-benar melayani evaluasi 25 Agustus 2026. Angka jejak memori pada
# Tabel VI.9 diukur pada lingkungan conda tersebut, bukan pada daftar ini.
#
# Berkas ini SENGAJA berbeda dari requirements.txt di root: root memuat pula
# dependensi reproduksi angka TA (TensorFlow, RAGAS, matplotlib) yang tidak
# dibutuhkan untuk melayani permintaan.
#
# Versi numpy/pandas/scikit-learn WAJIB identik dengan root. scikit-learn 1.3.0
# khususnya: bundle inferensi .pkl dibuat dengan versi itu dan tidak dapat dibuka
# oleh versi yang lebih baru.

# --- Web ---
fastapi==0.115.9
uvicorn==0.49.0
pydantic==2.13.4
python-dotenv==1.0.0

# --- Data & model prediksi ---
numpy==1.26.4
pandas==2.0.3
scikit-learn==1.3.0

# PyYAML dideklarasikan EKSPLISIT. src/config.py meng-import yaml dan berada pada
# jalur kritis backend (prediction_service dan clinical route). Sebelum ini paket
# tersebut hanya ikut terpasang sebagai dependensi transitif langchain-core —
# artinya backend bergantung pada daftar dependensi paket lain untuk bisa hidup,
# dan pembaruan langchain dapat mematikannya pada tahap import.
pyyaml==6.0.1

# --- Penelusuran (jalur produksi: rag.retrieval_mode = bm25) ---
# ChromaDB dipakai sebagai PENYIMPAN dokumen: _bangun_bm25 dan _hasil_dari_indeks
# hanya membaca documents/metadatas dari sqlite. Mesin vektornya tidak dijalankan.
chromadb==1.0.12
rank_bm25==0.2.2

# --- Generasi ---
langchain-core==0.3.86
langchain-google-genai>=2.0.0

# DICABUT 25 Agustus 2026 — jalur vektor dan penyusunan ulang tidak dipakai
# produksi, dan ketiadaannya di sini TIDAK mengubah satu baris pun kode.
#
#   flashrank==0.2.10            rag.reranker.enabled = false, dan _muat_reranker
#                                mengembalikan nilai pada penjaga `if not
#                                self.reranker_enabled` SEBELUM `from flashrank
#                                import Ranker`. Paket ini tidak pernah disentuh.
#                                Komentar lama yang menyebut "tanpa paket ini
#                                sistem menelusur TANPA reranking" sudah keliru.
#
#   langchain-chroma==0.2.4      Jalur vektor/MMR. Ditelusuri dengan jejak impor
#   langchain-community==0.3.24  pada konfigurasi produksi (bm25): keempatnya
#   langchain-text-splitters     TIDAK PERNAH di-import saat melayani permintaan.
#   sentence-transformers        Kodenya TETAP ADA di src/rag/retriever.py sebagai
#                                kontrol eksperimen Tabel VI.3; yang dicabut hanya
#                                dependensinya dari lingkungan penyajian.
#
# KONSEKUENSI YANG DIPILIH SADAR. Bila indeks BM25 gagal dibangun, retriever tidak
# lagi dapat turun ke jalur vektor. Ia turun ke SimpleKeywordRetriever atas 36
# potongan cadangan — jalur yang sudah terimplementasi, sudah membawa identitas
# dokumen dan nomor halaman, dan sudah mencatat peringatan keras. Yang hilang satu
# tingkat degradasi, bukan ketersediaan.
#
# EFEK SAMPING YANG DIHARAPKAN. langchain-core menyelidik `from transformers import
# GPT2TokenizerFast` di dalam try/except. Selama sentence-transformers terpasang,
# penyelidikan itu berhasil dan menyeret torch (~1 GB) ke dalam proses meski tidak
# ada satu pun jalur yang memakainya. Tanpa paket ini, ia mundur bersih.
#
# UNTUK MEMBANGUN ULANG INDEKS, pakai requirements.txt di root: scripts/reingest_kb.py
# dan MedicalKnowledgeBase.save_to_chroma() TETAP membutuhkan keempatnya.

# DICABUT 23 Agustus 2026, seluruhnya nol referensi di src/ dan backend/:
#   langchain==0.3.25          paket payung; hanya sub-paketnya yang di-import
#   langchain-openai>=0.3.0    hanya dipakai skrip RAGAS (pengembangan, bukan runtime)
#   pydantic-settings==2.14.2  nol import di seluruh repositori
#   python-dateutil            dibawa pandas
#   scipy==1.11.1              dibawa scikit-learn
#   nest-asyncio               hanya dipakai skrip RAGAS
