# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
ta/
ta1/
.ta1/
.venv/
venv/

# Keadaan setengah jadi dari sapuan panjang (checkpoint eval_rf_ukuran.py dsb).
# Sengaja di luar results/ supaya tidak pernah terbaca sebagai hasil.
.cache/

# Python build/test artifacts
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
dist/
build/
*.egg-info/

# Data
data/raw/*.csv
data/processed/*.csv
*.pkl
*.h5
*.xml

# Models
models/*.h5
models/*.pkl
models/*.gguf
# vector DB (generated; rebuild via scripts/reingest_kb.py)
models/chroma_db/
models/chroma_db_multilingual/
# Indeks sementara sapuan ukuran potongan (B4) — dibangun ulang oleh
# scripts/eval_chunk_size.py, tidak perlu masuk repo.
models/_chunk_sweep/
# Indeks sementara empat varian strategi chunking (T7) — dibangun ulang oleh
# scripts/eval_strategi_chunking.py, tidak perlu masuk repo.
models/_chunk_strategi/
# Koleksi evaluasi RAGAS (dibangun ulang via scripts/build_eval_kb.py)
models/chroma_db_eval/

# Conda env lokal (bila dibuat dengan --prefix di dalam repo)
diabetes-ta/
env/

# Output notebook (regenerate saat run)
notebooks/outputs/

# Logs
logs/*.log

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Jupyter
.ipynb_checkpoints/

# Environment
.env
config_local.yaml

# Streamlit
.streamlit/secrets.toml

# Large files
*.zip
*.tar.gz

# LaTeX build artifacts (regenerate via xelatex + biber)
*.aux
*.bbl
*.blg
*.bcf
*.log
*.out
*.toc
*.lof
*.lot
*.lol
*.loa
*.equ
*.synctex.gz
*.fls
*.fdb_latexmk
docs/laporan_TA/TA-STI-template-1.0/TA.pdf
*.synctex(busy)

# Dokumen berhak cipta / draft internal — tidak dipublikasikan di repo publik
docs/OhioT1DM-dataset-paper.pdf
docs/pape_iciss/
data/knowledge_base/additional_docs/
# Korpus pedoman KB-01..KB-12 (PDF berhak cipta PERKENI/IDAI/ADA/ISPAD/ATTD).
# manifest.csv TIDAK diabaikan: ia kontrak metadata halaman dan wajib ikut repo.
data/knowledge_base/books/
data/knowledge_base/tidak terpakai lagi/

# Dokumen kerja & bantu penulisan (tetap ada di lokal, tidak dipublikasikan).
# docs/proposal_ta memuat figur dari publikasi berhak cipta dan lembar pengesahan
# bertanda tangan, serta sudah tidak selaras dengan TA final.
docs/proposal_ta/
docs/METHODOLOGY.md
docs/journey/
docs/doc.md
docs/SUS_kuesioner.md
LEARN.md
LEARN.html
*.bbl-SAVE-ERROR